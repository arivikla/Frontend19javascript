/*
    C#
        Array:   public string[] names = new string[10];
        List:    public List<string> names = new List();
        
    JS
        Array:  let names = [];
        const name = []; (kan lägga till och ta bort i arrayen)

        const users = [
            { fistName: 'Ari', lastName: 'Vikla'},
            { fistName: 'Lena', lastName: 'Vikla'},
            { fistName: 'Ella', lastName: 'Vikla'},
        ];

        const names = ['Ari', 'Lena', 'Ella'];

        // Hämta information från en array
        console.log(names);  (Hämtar hela arrayen)
        console.log(names[0]); (Hämtar en specifik post från arrayen)
        console.log(names[1]);
        console.log(names[2]);

*/
